// import { ValueObject } from 'src/libs/value-object-base';

type AddressProps = {
  street: string;
  city: string;
  state: string;
  zipCode: string;
};

// export class Address extends ValueObject<>
